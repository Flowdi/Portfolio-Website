/* Generated by tools/build-bundle.mjs. Do not edit directly. */
(() => {
  "use strict";
  const modules = {"js/util/math.js": (require, exports) => {
function map(v, a1, a2, b1, b2) {
  return b1 + (b2 - b1) * (v - a1) / (a2 - a1);
}

Object.assign(exports, { map });

},
"js/i18n/bilingual.js": (require, exports) => {
// js/i18n/bilingual.js

const translations = {
  de: {
    ui: {
      boostHint: "Klicke auf die Sphere, um die Rotation zu beschleunigen",
      boostLabel: "Orbit beschleunigen",
    },

    orbitLabels: {
      contact: "Kontakt",
      experience: "Developer Journey",
      skills: "Skills",
      projects: "Projekte",
      certificates: "Zertifikate",
      about: "Über mich",
    },

    panels: {
      contact: {
        title: "Kontakt",
        intro:
          "Ich freue mich über jede Form von Kontakt rund um Webentwicklung, spannende Projekte oder fachlichen Austausch. Besonders interessieren mich Frontend-Entwicklung, das JavaScript-Ökosystem, moderne Webarchitektur und Clean Code.\n\nWenn du eine Idee, ein Projekt oder einfach nur eine Frage hast – schreib mir gern. Ich melde mich so schnell wie möglich zurück.",
        buttons: {
          email: "E-Mail",
          github: "GitHub",
          linkedin: "LinkedIn",
          xing: "Xing",
        },
      },

      experience: {
  title: "Developer Journey",
  items: [
    {
      phase: "education",
      heading: "IHK-Abschluss – Fachinformatiker für Anwendungsentwicklung",
      time: "2024 – 2026",
      text:
        "Erfolgreich abgeschlossene Umschulung mit fundierter Praxis in Programmierung, Datenbanken, Softwaredesign, Versionsverwaltung und projektorientierter Entwicklung. Die Ausbildung bildet das technische Fundament für meine weitere Spezialisierung."
    },
    {
      phase: "engineering",
      heading: "Software Engineering & projektbasiertes Lernen",
      time: "laufend",
      text:
        "Entwicklung realer Anwendungen mit Python und JavaScript entlang des gesamten Entwicklungszyklus: Anforderungsanalyse, Architektur, Implementierung, Testing, Debugging und Iteration. Git, modularer Code und nachvollziehbare Dokumentation gehören fest zu meinem Workflow."
    },
    {
      phase: "projects",
      heading: "Fokus: Python & Automatisierung",
      time: "aktueller Schwerpunkt",
      text:
        "Aufbau eines klaren Profils rund um Python, Skripting und die Automatisierung wiederkehrender Abläufe. Im Mittelpunkt stehen robuste Werkzeuge, Datenverarbeitung, API-Anbindungen und Lösungen, die manuelle Prozesse messbar vereinfachen."
    },
    {
      phase: "growth",
      heading: "Nächster Entwicklungsschritt: Data & Machine Learning",
      time: "im Aufbau",
      text:
        "Gezielte Vertiefung in Datenaufbereitung, Analyse und Machine-Learning-Grundlagen mit NumPy, Pandas und scikit-learn. Neue Konzepte werden in kleinen, nachvollziehbaren Projekten praktisch erprobt und schrittweise zu produktionsnahen Lösungen ausgebaut."
    }
  ]
}
,

      skills: {
        title: "Skills",
        groups: [
          { title: "Python & Automatisierung", items: ["Python", "Skripting", "Prozessautomatisierung", "REST APIs", "Datenverarbeitung"] },
          { title: "Data & Machine Learning – im Aufbau", items: ["Pandas", "NumPy", "scikit-learn", "Datenanalyse", "ML-Grundlagen"] },
          { title: "Software Engineering", items: ["Clean Code", "Git & GitHub", "Testing & Debugging", "Modulare Architektur", "SQL"] },
          { title: "Web-Grundlagen", items: ["HTML", "CSS", "JavaScript", "Node.js", "PostgreSQL"] },
          { title: "Tools", items: ["VS Code", "Jupyter", "Postman", "Docker", "Linux Basics"] },
          {
            title: "Soft Skills",
            items: ["Teamarbeit", "Kommunikation", "Problemlösung", "Eigenständiges Arbeiten", "Kreativität"],
          },
        ],
      },

      projects: {
        title: "Projekte",
        viewLabel: "Ansehen",
        openLabel: "Neu öffnen",
        backLabel: "Zurück zu den Projekten",
        repoLabel: "Repo",
        playLabel: "Spielen",
        builtLabel: "Built:",
        learnedLabel: "Learned:",
        items: [
          {
            id: "portfolio-v2",
            title: "Verbesserte Portfolio Website",
            desc: "Individuelle Portfolio-Website mit Space-UI, Animationen und modularem JavaScript.",
            tech: ["JavaScript", "HTML", "CSS", "GitHub"],
            preview: "assets/projects/portfolio-v2.jpg",
            url: "", // optional
            repoId: "portfolio-v2",
            built: "Canvas-Background, Orbit-UI, Panels, i18n (DE/EN), modulare Struktur",
            learned: "UI-State, Event Delegation, Performance/Rendering, saubere Modul-Trennung",
          },
          {
            id: "foodapp",
            title: "Food Tracking App",
            desc: "Web-App zur Übersicht vorhandener Lebensmittel im Haushalt.",
            tech: ["JavaScript", "HTML", "CSS", "PHP", "PostgreSQL"],
            preview: "assets/projects/flosfoodapp.jpg",
            url: "",
            repoId: "foodapp",
            built: "CRUD-Flow, Datenmodell, UI-Listen & Detailansichten",
            learned: "Datenhaltung, Formular-UX, strukturierter Backend/Frontend-Workflow",
          },
          {
            id: "portfolio-v1",
            title: "Portfolio Website (V1)",
            desc: "Ältere Portfolio-Website in schlichtem Design.",
            tech: ["JavaScript", "HTML", "CSS"],
            preview: "assets/projects/portfolio-v1.jpg",
            url: "assets/links/website2/index.html",
            repoId: "portfolio-v1",
            built: "Mehrseitiges Layout, UI-Komponenten, grundlegende Animationen",
            learned: "Layouting, CSS-Struktur, sauberes Refactoring in V2",
          },
          {
            id: "kot-game",
            title: "'Kot'-Game",
            desc: "Ein 1-Level Platformer Spiel das mit JavaScript geschrieben wurde.",
            tech: ["JavaScript", "HTML", "CSS"],
            preview: "assets/projects/kotgame.jpg",
            url: "assets/links/kotgame/index.html",
            repoId: "kot-game",
            type: "game",
            built: "Player Movement, Collision, Level-Loop, Input Handling",
            learned: "Game Loop, Timing, Debugging von Physik & Zuständen",
          },
          {
            id: "website1",
            title: "Erste eigene Website",
            desc: "Meine erste eigene Website um erlerntes anzuwenden.",
            tech: ["HTML", "CSS"],
            preview: "assets/projects/website1.jpg",
            url: "assets/links/website1/index.html",
            repoId: "website1",
          },
        ],
      },

      // central placed repo links
      repos: {
        placeholder: "",
        items: {
          "portfolio-v2": "https://github.com/Flowdi/portfolio-v2",
          "foodapp": "https://github.com/Flowdi/FlosFoodApp",
          "portfolio-v1": "https://github.com/Flowdi/portfolio-v1",
          "kot-game": "https://github.com/Flowdi/kotgame",
          "website1": "https://github.com/Flowdi/website1",
        },
      },

      certificates: {
        title: "Zertifikate",
        sections: { earned: "Abgeschlossene Zertifikate", progress: "In Bearbeitung", upcoming: "Geplant" },
        buttons: { view: "Ansehen", verify: "Verifizieren", back: "Zurück", previous: "Vorheriges Zertifikat", next: "Nächstes Zertifikat" },
        viewer: { headline: "Zertifikat", verifyHint: "Verifikation öffnen" },
        items: {
          earned: [
            {
              id: "ihk-fiae",
              title: "Fachinformatiker – Anwendungsentwicklung",
              issuer: "IHK Schwaben",
              year: "2026",
              img: "assets/certificates/ihk-fiae-de.png",
              verifyUrl: "#",
            },
            {
              id: "fcc-js",
              title: "JavaScript Algorithms & Data Structures",
              issuer: "freeCodeCamp",
              year: "2024",
              img: "assets/certificates/fcc-js.png",
              verifyUrl: "#",
            },
            {
              id: "fcc-responsive",
              title: "Responsive Web Design",
              issuer: "freeCodeCamp",
              year: "2024",
              img: "assets/certificates/fcc-responsive.png",
              verifyUrl: "#",
            },
          ],
          progress: [
            { id: "python-automation", title: "Python, Automatisierung & Machine Learning", issuer: "Weiterbildung und eigene Projekte", year: "2026", status: "In Bearbeitung" },
          ],
          upcoming: [],
        },
      },

      about: {
        title: "Über mich",
        paragraphs: [
          "Ich bin ausgebildeter Fachinformatiker für Anwendungsentwicklung (IHK) und richte meinen fachlichen Schwerpunkt auf Python, Automatisierung und datengetriebene Softwarelösungen.",
          "Mein Ziel ist es, wiederkehrende Prozesse mit sauberem Code und praxistauglichen Werkzeugen zu vereinfachen. Parallel vertiefe ich Datenanalyse und Machine Learning, um Automatisierung künftig mit intelligenten Verfahren zu verbinden.",
          "Abseits des Bildschirms beschäftige ich mich gerne mit Sport, Wandern und kreativen Projekten, um neue Impulse zu sammeln.",
        ],
      },
    },
  },

  en: {
    ui: {
      boostHint: "Click the sphere to boost the rotation",
      boostLabel: "Boost orbit rotation",
    },

    orbitLabels: {
      contact: "Contact",
      experience: "Developer Journey",
      skills: "Skills",
      projects: "Projects",
      certificates: "Certificates",
      about: "About Me",
    },

    panels: {
      contact: {
        title: "Contact",
        intro:
          "I’m always happy to connect about web development, exciting projects or professional exchange. My main interests are front-end development, the JavaScript ecosystem, modern web architecture and clean code.\n\nIf you have an idea, a project or simply a question – feel free to reach out.",
        buttons: {
          email: "Email",
          github: "GitHub",
          linkedin: "LinkedIn",
          xing: "Xing",
        },
      },

      experience: {
  title: "Developer Journey",
  items: [
    {
      phase: "education",
      heading: "IHK Graduation – Application Development Specialist",
      time: "2024 – 2026",
      text:
        "Successfully completed professional retraining with practical foundations in programming, databases, software design, version control and project-based development. This qualification provides the technical foundation for my ongoing specialization."
    },
    {
      phase: "engineering",
      heading: "Software Engineering & Project-Based Learning",
      time: "ongoing",
      text:
        "Building real applications with Python and JavaScript across the full development cycle: requirements, architecture, implementation, testing, debugging and iteration. Git, modular code and clear documentation are integral parts of my workflow."
    },
    {
      phase: "projects",
      heading: "Focus: Python & Automation",
      time: "current focus",
      text:
        "Developing a clear profile around Python, scripting and the automation of recurring workflows. The focus is on robust tools, data processing, API integrations and solutions that measurably reduce manual work."
    },
    {
      phase: "growth",
      heading: "Next Step: Data & Machine Learning",
      time: "in development",
      text:
        "Focused learning in data preparation, analysis and machine-learning fundamentals with NumPy, Pandas and scikit-learn. New concepts are tested in small, transparent projects and gradually developed into production-oriented solutions."
    }
  ]
}
,

      skills: {
        title: "Skills",
        groups: [
          { title: "Python & Automation", items: ["Python", "Scripting", "Process Automation", "REST APIs", "Data Processing"] },
          { title: "Data & Machine Learning – developing", items: ["Pandas", "NumPy", "scikit-learn", "Data Analysis", "ML Fundamentals"] },
          { title: "Software Engineering", items: ["Clean Code", "Git & GitHub", "Testing & Debugging", "Modular Architecture", "SQL"] },
          { title: "Web Foundations", items: ["HTML", "CSS", "JavaScript", "Node.js", "PostgreSQL"] },
          { title: "Tools", items: ["VS Code", "Jupyter", "Postman", "Docker", "Linux Basics"] },
          {
            title: "Soft Skills",
            items: ["Teamwork", "Communication", "Problem Solving", "Self-directed Work", "Creativity"],
          },
        ],
      },

      projects: {
        title: "Projects",
        viewLabel: "View",
        openLabel: "Open",
        backLabel: "Back to projects",
        repoLabel: "Repo",
        playLabel: "Play",
        builtLabel: "Built:",
        learnedLabel: "Learned:",
        items: [
          {
            id: "portfolio-v2",
            title: "Enhanced Portfolio Website",
            desc: "Custom portfolio website with space UI, animations and modular JavaScript.",
            tech: ["JavaScript", "HTML", "CSS", "GitHub"],
            preview: "assets/projects/portfolio-v2.jpg",
            url: "",
            repoId: "portfolio-v2",
            built: "Canvas background, orbit UI, panels, i18n (DE/EN), modular structure",
            learned: "UI state, event delegation, performance/rendering, clean module separation",
          },
          {
            id: "foodapp",
            title: "Food Tracking App",
            desc: "Web app to track available food items at home.",
            tech: ["JavaScript", "HTML", "CSS", "PHP", "PostgreSQL"],
            preview: "assets/projects/flosfoodapp.jpg",
            url: "",
            repoId: "foodapp",
            built: "CRUD flow, data model, UI lists & detail views",
            learned: "data handling, form UX, structured backend/frontend workflow",
          },
          {
            id: "portfolio-v1",
            title: "Portfolio Website (V1)",
            desc: "Older portfolio website with a simpler design.",
            tech: ["JavaScript", "HTML", "CSS"],
            preview: "assets/projects/portfolio-v1.jpg",
            url: "assets/links/website2/index.html",
            repoId: "portfolio-v1",
            built: "Multi-page layout, UI components, basic animations",
            learned: "layouting, CSS structure, refactoring improvements in V2",
          },
          {
            id: "kot-game",
            title: "'Kot' Game",
            desc: "A 1-level platformer game built with JavaScript.",
            tech: ["JavaScript", "HTML", "CSS"],
            preview: "assets/projects/kotgame.jpg",
            url: "assets/links/kotgame/index.html",
            repoId: "kot-game",
            type: "game",
            built: "player movement, collision, level loop, input handling",
            learned: "game loop, timing, debugging physics/state issues",
          },
          {
            id: "website1",
            title: "First own Website",
            desc: "My first own website using skills I had just learned.",
            tech: ["HTML", "CSS"],
            preview: "assets/projects/website1.jpg",
            url: "assets/links/website1/index.html",
            repoId: "website1",
          },
        ],
      },

      repos: {
        placeholder: "",
        items: {
          "portfolio-v2": "https://github.com/Flowdi/portfolio-v2",
          "foodapp": "https://github.com/Flowdi/FlosFoodApp",
          "portfolio-v1": "https://github.com/Flowdi/portfolio-v1",
          "kot-game": "https://github.com/Flowdi/kotgame",
          "website1": "https://github.com/Flowdi/website1",
        },
      },

      certificates: {
        title: "Certificates",
        sections: { earned: "Earned Certificates", progress: "In Progress", upcoming: "Upcoming" },
        buttons: { view: "View", verify: "Verify", back: "Back", previous: "Previous certificate", next: "Next certificate" },
        viewer: { headline: "Certificate", verifyHint: "Open verification" },
        items: {
          earned: [
            {
              id: "ihk-fiae",
              title: "Computer Science Expert – Software Development",
              issuer: "IHK Schwaben",
              year: "2026",
              img: "assets/certificates/ihk-fiae-en.png",
              verifyUrl: "#",
            },
            {
              id: "fcc-js",
              title: "JavaScript Algorithms & Data Structures",
              issuer: "freeCodeCamp",
              year: "2024",
              img: "assets/certificates/fcc-js.png",
              verifyUrl: "#",
            },
            {
              id: "fcc-responsive",
              title: "Responsive Web Design",
              issuer: "freeCodeCamp",
              year: "2024",
              img: "assets/certificates/fcc-responsive.png",
              verifyUrl: "#",
            },
          ],
          progress: [
            { id: "python-automation", title: "Python, Automation & Machine Learning", issuer: "Continuing education and own projects", year: "2026", status: "In Progress" },
          ],
          upcoming: [],
        },
      },

      about: {
        title: "About Me",
        paragraphs: [
          "I am an IHK-certified application development specialist focusing on Python, automation and data-driven software solutions.",
          "My goal is to simplify recurring processes with clean code and practical tools. At the same time, I am developing my data analysis and machine-learning skills to connect automation with intelligent methods.",
          "Outside the screen I enjoy sports, hiking and creative activities to gain new inspiration.",
        ],
      },
    },
  },
};

// internal language state
const state = {
  lang: "de",
};

function setLang(lang) {
  if (!translations[lang]) return;
  state.lang = lang;
  document.documentElement.lang = lang;
}

function getLang() {
  return state.lang;
}

function tPanel(panelId) {
  return translations[state.lang].panels[panelId];
}

function tOrbitLabel(orbitId) {
  return translations[state.lang].orbitLabels[orbitId] || orbitId;
}

function tUI(key) {
  return translations[state.lang].ui?.[key] || key;
}

// Repo resolver: return null => hide button
function tRepo(repoId) {
  const p = translations[state.lang].panels;
  const url = p?.repos?.items?.[repoId] ?? p?.repos?.placeholder ?? "";
  if (!url || url === "#") return null;
  return url;
}

Object.assign(exports, { setLang, getLang, tPanel, tOrbitLabel, tUI, tRepo });

},
"js/input/controls.js": (require, exports) => {
function initBoostControls(starBtn, boostHint, setTargetSpeed) {
  if (!starBtn) return;
  function activate() {
    setTargetSpeed(4);
    if (boostHint) boostHint.style.display = "none";
  }

  function deactivate() {
    setTargetSpeed(1);
  }

  starBtn.addEventListener("pointerdown", (event) => {
    starBtn.setPointerCapture?.(event.pointerId);
    activate();
  });
  starBtn.addEventListener("pointerup", deactivate);
  starBtn.addEventListener("pointercancel", deactivate);
  starBtn.addEventListener("lostpointercapture", deactivate);
  starBtn.addEventListener("keydown", (event) => {
    if (event.key === "Enter" || event.key === " ") activate();
  });
  starBtn.addEventListener("keyup", (event) => {
    if (event.key === "Enter" || event.key === " ") deactivate();
  });
}

Object.assign(exports, { initBoostControls });

},
"js/orbit/orbit-ui.js": (require, exports) => {
const { map } = require("js/util/math.js");
const { tOrbitLabel } = require("js/i18n/bilingual.js");
// constant orbit-parameters
const orbitSpeedBase = 0.0009;

function getOrbitRadius() {
  return Math.max(105, Math.min(260, Math.min(window.innerWidth, window.innerHeight) * 0.34));
}

// Orbit Items
const data = [
  { id: "contact",      img: "assets/images/contact.png" },
  { id: "experience",   img: "assets/images/experience.png" },
  { id: "skills",       img: "assets/images/skills.png" },
  { id: "projects",     img: "assets/images/projects.png" },
  { id: "certificates", img: "assets/images/certificates.png" },
  { id: "about",        img: "assets/images/about.png" }
];


let orbitItems = [];
let orbitRotation = 0;
let onSelect = null;

/**
 * initialises the orbit ui and creates clickable orbit items
 *
 * @param {HTMLElement} orbitContainer - DOM-Element, in which the orbit items get pasted in
 * @param {(id: string) => void} onItemSelect - Callback, that gets calles by clicking on an icon
 */

function initOrbit(orbitContainer, onItemSelect) {
  if (!orbitContainer) {
    console.warn("initOrbit: orbitContainer ist null/undefined");
    return;
  }

  orbitItems = [];
  orbitRotation = 0;
  onSelect = onItemSelect || null;

  data.forEach((item, i) => {
    const el = document.createElement("button");
    el.type = "button";
    el.className = "orbit-item";
el.innerHTML = `
  <div class="orbit-item-inner">
    <div class="orbit-wobble">
      <div class="icon">
        <img src="${item.img}" alt="" width="90" height="90" draggable="false">
      </div>
      <div class="label">${tOrbitLabel(item.id)}</div>
    </div>
  </div>
`;



    el.addEventListener("click", () => {
      if (onSelect) onSelect(item.id);
    });

    orbitContainer.appendChild(el);

    orbitItems.push({
      el,
      id: item.id,
      angle: (i / data.length) * Math.PI * 2
    });
  });
}


/**
 * updates position, scaling and layer-order of the orbit icons
 * based on the current speedfactor of the scene
 *
 * @param {number} speedFactor - 1 = normal speed, >1 = boosted speed
 */
function updateOrbit(speedFactor) {

  if (orbitItems.length === 0) return;

  orbitRotation += orbitSpeedBase * speedFactor;

  const orbitRadius = getOrbitRadius();
  orbitItems.forEach(o => {
    const a = o.angle + orbitRotation;
    const x = Math.sin(a) * orbitRadius;
    const y = Math.cos(a) * orbitRadius * 0.35;

    const scale = map(y, -orbitRadius, orbitRadius, 0.6, 1.35);

    o.el.style.transform = `translate(${x}px, ${y}px) scale(${scale})`;
    o.el.style.zIndex = Math.floor(scale * 1000);

    if (scale > 1.15) {
      o.el.classList.add("active");
    } else {
      o.el.classList.remove("active");
    }
  });
}

/**
 * refreshes the orbit labels for the speechoptions
 */
function refreshOrbitLabels() {
  orbitItems.forEach((o) => {
    const labelEl = o.el.querySelector(".label");
    if (labelEl) {
      labelEl.textContent = tOrbitLabel(o.id);
      o.el.setAttribute("aria-label", tOrbitLabel(o.id));
    }
  });
}

Object.assign(exports, { initOrbit, updateOrbit, refreshOrbitLabels });

},
"js/background/stars.js": (require, exports) => {
let stars = [];

/**
 *
 * @param {number} width - initialises the stars in the background
 * @param {number} height - initialises the stars in the background
 */
function initStars(width, height) {
  stars = Array.from({ length: 320 }, () => ({
    x: Math.random() * width,
    y: Math.random() * height,
    r: Math.random() * 1.4,
    s: Math.random() * 0.4 + 0.2
  }));
}

/**
 *
 * @param {CanvasRenderingContext2D} ctx - 2D rendering context of the canvas
 * @param {number} speedFactor - factor, how fast the stars move(1 = normal)
 * @param {number} width - current canvas width
 * @param {number} height - current canvas height
 */
function drawStars(ctx, speedFactor, width, height) {
  ctx.save();
  ctx.fillStyle = "white";

  stars.forEach(s => {
    s.x -= s.s * speedFactor * 1.5;
    if (s.x < 0) s.x = width;

    ctx.globalAlpha = Math.min(1, speedFactor * 0.4);
    ctx.beginPath();
    ctx.arc(s.x, s.y, s.r, 0, Math.PI * 2);
    ctx.fill();
  });

  ctx.restore();
}

Object.assign(exports, { initStars, drawStars });

},
"js/background/nebula.js": (require, exports) => {
// js/background/nebula.js

const nebulaColors = [220, 250, 280];
let nebulae = [];
let canvasWidth = 0;
let canvasHeight = 0;

/**
 *
 * @param {number} width - canvaswidth
 * @param {number} height - canvasheight
 */

function initNebula(width, height) {
  canvasWidth = width;
  canvasHeight = height;

  nebulae = Array.from({ length: 5 }, (_, i) => ({
    x: Math.random() * width,
    y: Math.random() * height,
    r: Math.random() * 400 + 260,
    dx: (Math.random() - 0.5) * 0.005,
    dy: (Math.random() - 0.5) * 0.005,
    hue: nebulaColors[i % nebulaColors.length]
  }));
}

function resizeNebula(width, height) {
  canvasWidth = width;
  canvasHeight = height;
}

/**
 * renders the fog in the background so background isnt just black
 * @param {CanvasRenderingContext2D} ctx - 2D Rendering context of the canvas
 * @param {number} speedFactor - speed of the fog movement
 */

function drawNebula(ctx, speedFactor) {
  ctx.save();
  ctx.globalCompositeOperation = "lighter";

  ctx.filter = "blur(80px)";

  nebulae.forEach(n => {
    n.x += n.dx;
    n.y += n.dy;

    const w = canvasWidth;
    const h = canvasHeight;

    if (n.x < -n.r) n.x = w + n.r;
    if (n.x > w + n.r) n.x = -n.r;
    if (n.y < -n.r) n.y = h + n.r;
    if (n.y > h + n.r) n.y = -n.r;

    ctx.fillStyle = `hsla(${n.hue},70%,55%,0.08)`;
    ctx.beginPath();
    ctx.arc(n.x, n.y, n.r, 0, Math.PI * 2);
    ctx.fill();
  });

  ctx.restore();
}


Object.assign(exports, { initNebula, resizeNebula, drawNebula });

},
"js/background/meteors.js": (require, exports) => {
let meteors = [];
let getSize = () => ({ width: 0, height: 0 });

/**
 * Renders the size of the meteors
 * @param {number} sizeProvider
 */

function initMeteors(sizeProvider) {
  getSize = sizeProvider;

  setInterval(() => {
    const { width, height } = getSize();
    if (!Number.isFinite(width) || !Number.isFinite(height)) return;
    meteors.push({
      x: Math.random() * width,
      y: Math.random() * height * 0.4,
      vx: Math.random() * 8 + 8,
      vy: Math.random() * 3 + 3,
      life: 0
    });
  }, 5000);
}

/**
 *
 * @param {CanvasRenderingContext2D} ctx - renders the meteors that appear in the background
 * @param {number} speedFactor - renders the speed of the meteors
 */
function drawMeteors(ctx, speedFactor) {
  ctx.save();
  ctx.globalCompositeOperation = "lighter";

  for (let i = meteors.length - 1; i >= 0; i--) {
    const m = meteors[i];

    ctx.strokeStyle = "rgba(255,255,255,0.8)";
    ctx.lineWidth = 2;
    ctx.beginPath();
    ctx.moveTo(m.x, m.y);
    ctx.lineTo(
      m.x - m.vx * speedFactor * 3,
      m.y - m.vy * speedFactor * 3
    );
    ctx.stroke();

    m.x += m.vx * speedFactor;
    m.y += m.vy * speedFactor;
    m.life++;

    if (m.life > 60) {
      meteors.splice(i, 1);
    }
  }

  ctx.restore();
}


Object.assign(exports, { initMeteors, drawMeteors });

},
"js/boost/boost-core.js": (require, exports) => {
let boostCanvas;
let bctx;
let bw, bh;

const coreParticles = [];
const coreParticleCount = 220;
let corePulse = 0;

// fit canvas size
function resizeBoost() {
  if (!boostCanvas) return;
  const size = 180;
  const dpr = Math.min(window.devicePixelRatio || 1, 2);
  bw = boostCanvas.width = size * dpr;
  bh = boostCanvas.height = size * dpr;
  bctx?.setTransform(dpr, 0, 0, dpr, 0, 0);
  bw = bh = size;
  boostCanvas.style.width = size + "px";
  boostCanvas.style.height = size + "px";
}

// 3D-rotation
function rotate3D(p, angleX, angleY, angleZ) {
  let x = p.x, y = p.y, z = p.z;

  let y1 = y * Math.cos(angleX) - z * Math.sin(angleX);
  let z1 = y * Math.sin(angleX) + z * Math.cos(angleX);
  y = y1; z = z1;

  let x1 = x * Math.cos(angleY) + z * Math.sin(angleY);
  let z2 = -x * Math.sin(angleY) + z * Math.cos(angleY);
  x = x1; z = z2;

  let x2 = x * Math.cos(angleZ) - y * Math.sin(angleZ);
  let y2 = x * Math.sin(angleZ) + y * Math.cos(angleZ);
  x = x2; y = y2;

  return { x, y, z };
}
/**
 *
 * @param {*} canvasElement
 */
function initBoostCore(canvasElement) {
  boostCanvas = canvasElement;
  if (!boostCanvas) {
    console.warn("initBoostCore: canvasElement fehlt");
    return;
  }

  bctx = boostCanvas.getContext("2d");
  resizeBoost();
  window.addEventListener("resize", resizeBoost);

  // Partikel erzeugen
  coreParticles.length = 0;
  for (let i = 0; i < coreParticleCount; i++) {
    const theta = Math.random() * Math.PI * 2;
    const phi = Math.acos(2 * Math.random() - 1);
    const r = Math.pow(Math.random(), 1.8) * 45;

    coreParticles.push({
      x: r * Math.sin(phi) * Math.cos(theta),
      y: r * Math.sin(phi) * Math.sin(theta),
      z: r * Math.cos(phi),
      size: 1 + Math.random() * 1.5,
      twinkleSpeed: 0.5 + Math.random() * 1.0,
      angleOffset: Math.random() * Math.PI * 2
    });
  }
}

/**
 *
 * @param {number} speedFactor - initialises the speed and strength of the boost effect
 */
function drawBoostCore(speedFactor) {
  if (!bctx) return;

  bctx.clearRect(0, 0, bw, bh);

  const cx = bw / 2;
  const cy = bh / 2;

  corePulse += 0.003 + speedFactor * 0.006;

  const angleX = corePulse * 0.08;
  const angleY = corePulse * 0.06;
  const angleZ = corePulse * 0.04;

  bctx.save();
  bctx.globalCompositeOperation = "lighter";

  const sphereRadius = 70;

  // Halo
  const haloGrad = bctx.createRadialGradient(
    cx, cy, sphereRadius * 0.7,
    cx, cy, sphereRadius * 1.3
  );
  haloGrad.addColorStop(0, "rgba(0, 200, 255, 0.35)");
  haloGrad.addColorStop(0.5, "rgba(0, 150, 255, 0.18)");
  haloGrad.addColorStop(1, "rgba(0, 80, 200, 0)");
  bctx.fillStyle = haloGrad;
  bctx.beginPath();
  bctx.arc(cx, cy, sphereRadius * 1.3, 0, Math.PI * 2);
  bctx.fill();

  // Inner Glow
  const innerGrad = bctx.createRadialGradient(
    cx, cy, 0,
    cx, cy, sphereRadius * 0.9
  );
  innerGrad.addColorStop(0, "rgba(255,255,255,0.95)");
  innerGrad.addColorStop(0.25, "rgba(180,220,255,0.8)");
  innerGrad.addColorStop(0.6, "rgba(40,140,255,0.5)");
  innerGrad.addColorStop(1, "rgba(0,40,120,0.0)");
  bctx.fillStyle = innerGrad;
  bctx.beginPath();
  bctx.arc(cx, cy, sphereRadius * 0.9, 0, Math.PI * 2);
  bctx.fill();

  // Particle
  for (const p of coreParticles) {
    const pos = rotate3D(p, angleX, angleY, angleZ);

    const perspective = 220 / (220 + pos.z);
    const x2d = cx + pos.x * perspective;
    const y2d = cy + pos.y * perspective;

    const baseSize = p.size * 0.7;
    const size =
      baseSize *
      (0.85 + 0.25 * Math.sin(corePulse * p.twinkleSpeed + p.angleOffset));

    const twinkle =
      0.55 + 0.45 * Math.sin(corePulse * p.twinkleSpeed * 2 + p.angleOffset);
    const alpha = 0.3 + 0.5 * twinkle;

    const grad = bctx.createRadialGradient(x2d, y2d, 0, x2d, y2d, size * 2.2);
    grad.addColorStop(0, `rgba(255,255,255,${alpha})`);
    grad.addColorStop(0.4, `rgba(210,230,255,${alpha * 0.6})`);
    grad.addColorStop(1, "rgba(210,230,255,0)");

    bctx.fillStyle = grad;
    bctx.beginPath();
    bctx.arc(x2d, y2d, size, 0, Math.PI * 2);
    bctx.fill();
  }

  bctx.restore();
}

Object.assign(exports, { initBoostCore, drawBoostCore });

},
"js/ui/panels.js": (require, exports) => {
// js/ui/panels.js
const { tPanel, tRepo } = require("js/i18n/bilingual.js");
let overlay;
let closeBtn;
let overlayBody;

let currentPanelId = null;
let previouslyFocused = null;

/* =========================================
   Panel Renderers
========================================= */

const panelRenderers = {
  contact: () => {
    const t = tPanel("contact");
    const introHtml = toParagraphHtml(t.intro);

    return `
      <h2>${escapeHtml(t.title)}</h2>
      <p>${introHtml}</p>

      <div class="contact-buttons">
        <a href="https://github.com/Flowdi" target="_blank" rel="noopener" class="btn outline">
          ${escapeHtml(t.buttons.github)}
        </a>
      </div>
    `;
  },

  experience: () => {
  const t = tPanel("experience");

  // Timeline Icon-Mapping
  const phaseIcons = {
    engineering: "assets/icons/timeline/engineering.png",
    projects: "assets/icons/timeline/projects.png",
    education: "assets/icons/timeline/education.png",
    growth: "assets/icons/timeline/growth.png",
  };

  return `
    <h2>${escapeHtml(t.title)}</h2>

    <div class="timeline space-timeline">
      ${(t.items || [])
        .map((item) => {
          const phase = item.phase || "engineering";
          const iconSrc = phaseIcons[phase] || phaseIcons.engineering;

          return `
            <div class="timeline-item phase-${escapeAttr(phase)}">
              <div class="timeline-node" aria-hidden="true">
                <img class="timeline-icon-img"
                     src="${escapeAttr(iconSrc)}"
                     alt=""
                     draggable="false">
              </div>

              <div class="timeline-content">
                <h3>${escapeHtml(item.heading)}</h3>
                <span class="time">${escapeHtml(item.time)}</span>
                <p>${escapeHtml(item.text)}</p>
              </div>
            </div>
          `;
        })
        .join("")}
    </div>
  `;
},

  skills: () => {
    const t = tPanel("skills");

    return `
      <h2>${escapeHtml(t.title)}</h2>
      ${(t.groups || []).map((g) => renderSkillGroup(g.title, g.items)).join("")}
    `;
  },

  projects: () => {
    const t = tPanel("projects");

    return `
      <h2>${escapeHtml(t.title)}</h2>

      <div class="project-grid">
        ${(t.items || []).map((p) => renderProjectCard(p, t)).join("")}
      </div>

      <div class="project-viewer" hidden>
        <div class="project-viewer-bar">
          <button type="button" class="btn outline small project-viewer-back">${escapeHtml(t.backLabel)}</button>
          <button type="button" class="btn outline small project-open-btn">${escapeHtml(t.openLabel)}</button>
        </div>

        <iframe class="project-frame" title="Project Preview" sandbox="allow-scripts allow-forms allow-modals"></iframe>
      </div>
    `;
  },

  certificates: () => {
    const t = tPanel("certificates");

    const earnedHtml = (t.items.earned || [])
      .map(
        (c) => `
        <div class="cert-card">
          <div class="cert-badge">
            <img src="${escapeAttr(c.img)}" alt="${escapeAttr(c.title)}">
          </div>

          <div class="cert-info">
            <h4>${escapeHtml(c.title)}</h4>
            <span class="issuer">${escapeHtml(c.issuer)}</span>
            <span class="year">${escapeHtml(c.year)}</span>
          </div>

          <div class="cert-actions">
            <button type="button"
              class="btn small"
              data-action="cert-view"
              data-cert-id="${escapeAttr(c.id)}">
              ${escapeHtml(t.buttons.view)}
            </button>

            ${
              c.verifyUrl && c.verifyUrl !== "#"
                ? `
                <a href="${escapeAttr(c.verifyUrl)}"
                  class="btn small outline"
                  target="_blank" rel="noopener">
                  ${escapeHtml(t.buttons.verify)}
                </a>
              `
                : ""
            }
          </div>
        </div>
      `
      )
      .join("");

    const progressHtml = (t.items.progress || [])
      .map(
        (c) => `
        <div class="cert-card in-progress">
          <div class="cert-badge placeholder"><span>Coming Soon</span></div>

          <div class="cert-info">
            <h4>${escapeHtml(c.title)}</h4>
            <span class="issuer">${escapeHtml(c.issuer)}</span>
            <span class="year">${escapeHtml(c.year)}</span>
          </div>

          <div class="cert-status in-progress-status">
            ${escapeHtml(c.status)}
          </div>
        </div>
      `
      )
      .join("");

    const upcomingHtml = (t.items.upcoming || [])
      .map(
        (c) => `
        <div class="cert-card upcoming">
          <div class="cert-badge placeholder"><span>${escapeHtml(c.issuer)}</span></div>

          <div class="cert-info">
            <h4>${escapeHtml(c.title)}</h4>
            <span class="issuer">${escapeHtml(c.issuer)}</span>
            <span class="year">${escapeHtml(c.year)}</span>
          </div>

          <div class="cert-status upcoming-status">
            ${escapeHtml(c.status)}
          </div>
        </div>
      `
      )
      .join("");

    return `
      <h2>${escapeHtml(t.title)}</h2>

      <div class="cert-viewer" id="certViewer" aria-hidden="true" hidden>
        <div class="cert-viewer-top">
          <button type="button" class="btn small outline" data-action="cert-back">
            ${escapeHtml(t.buttons.back)}
          </button>

          <div class="cert-viewer-meta">
            <div class="cert-viewer-kicker">${escapeHtml(t.viewer.headline)}</div>
            <div class="cert-viewer-title" id="certViewerTitle"></div>
            <div class="cert-viewer-sub" id="certViewerSub"></div>
          </div>

          <div class="cert-nav">
            <button type="button" class="btn small outline" data-action="cert-prev" aria-label="${escapeAttr(t.buttons.previous || "Previous")}">‹</button>
            <button type="button" class="btn small outline" data-action="cert-next" aria-label="${escapeAttr(t.buttons.next || "Next")}">›</button>
            <a class="btn small" id="certViewerVerify" href="#" target="_blank" rel="noopener">
              ${escapeHtml(t.viewer.verifyHint)}
            </a>
          </div>
        </div>

        <div class="cert-viewer-media" id="certViewerMedia">
          <img id="certViewerImg" alt="">
        </div>
      </div>

      <div class="cert-lightbox" id="certLightbox" aria-hidden="true" hidden>
        <div class="cert-lightbox-inner">
          <img id="certLightboxImg" alt="">
        </div>
      </div>

      <div class="cert-list" id="certList">
        <div class="cert-section">
          <h3 class="cert-title">${escapeHtml(t.sections.earned)}</h3>
          <div class="cert-grid">${earnedHtml}</div>

          ${progressHtml ? `
            <h3 class="cert-title">${escapeHtml(t.sections.progress)}</h3>
            <div class="cert-grid">${progressHtml}</div>
          ` : ""}

          ${upcomingHtml ? `
            <h3 class="cert-title">${escapeHtml(t.sections.upcoming)}</h3>
            <div class="cert-grid">${upcomingHtml}</div>
          ` : ""}
        </div>
      </div>
    `;
  },

  about: () => {
    const t = tPanel("about");

    return `
      <h2>${escapeHtml(t.title)}</h2>

      <div class="about-layout">
        <div class="about-image">
          <img src="assets/images/about-me.jpg" alt="Florian, Webentwickler" loading="lazy" width="180" height="180">
        </div>

        <div class="about-text">
          ${(t.paragraphs || []).map((p) => `<p>${escapeHtml(p)}</p>`).join("")}
        </div>
      </div>
    `;
  },
};

/* =========================================
   Helper functions (Templates)
========================================= */

function renderSkillGroup(title, items) {
  return `
    <div class="skill-group">
      <h3>${escapeHtml(title)}</h3>
      <div class="skill-chips">
        ${(items || []).map((i) => `<span class="chip">${escapeHtml(i)}</span>`).join("")}
      </div>
    </div>
  `;
}

function renderProjectCard(project, t) {
  const hasUrl = !!project.url;

  // Repo automatic of i18n resolver
  const repoUrl = project.repoId ? tRepo(project.repoId) : null;
  const hasRepo = !!repoUrl;

  const primaryLabel = project.type === "game" ? t.playLabel : t.viewLabel;

  return `
    <div class="project-card" data-project="${escapeAttr(project.id || "")}">
      ${project.preview ? `
        <div class="project-preview">
          <img src="${escapeAttr(project.preview)}" alt="${escapeAttr(project.title)}" loading="lazy" width="320" height="140">
        </div>
      ` : ""}

      <div class="project-body">
        <h3>${escapeHtml(project.title)}</h3>
        <p>${escapeHtml(project.desc)}</p>

        ${project.built ? `
          <p class="project-meta">
            <span class="meta-label">${escapeHtml(t.builtLabel)}</span>
            ${escapeHtml(project.built)}
          </p>
        ` : ""}

        ${project.learned ? `
          <p class="project-meta">
            <span class="meta-label">${escapeHtml(t.learnedLabel)}</span>
            ${escapeHtml(project.learned)}
          </p>
        ` : ""}

        <div class="tags">
          ${(project.tech || []).map((tag) => `<span class="chip">${escapeHtml(tag)}</span>`).join("")}
        </div>

        <div class="project-actions">
          ${hasUrl ? `
            <button type="button" class="btn small project-view-btn" data-url="${escapeAttr(project.url)}">
              ${escapeHtml(primaryLabel)}
            </button>
          ` : ""}

          ${hasRepo ? `
            <a class="btn small outline" href="${escapeAttr(repoUrl)}" target="_blank" rel="noopener">
              ${escapeHtml(t.repoLabel)}
            </a>
          ` : ""}
        </div>
      </div>
    </div>
  `;
}

// changes "\n\n" to <br><br>
function toParagraphHtml(text) {
  if (!text) return "";
  return escapeHtml(text).replace(/\n\n/g, "<br><br>").replace(/\n/g, "<br>");
}

// Escape for HTML-textnodes
function escapeHtml(str) {
  return String(str)
    .replaceAll("&", "&amp;")
    .replaceAll("<", "&lt;")
    .replaceAll(">", "&gt;")
    .replaceAll('"', "&quot;")
    .replaceAll("'", "&#039;");
}

// Escape for attributes (href, src, data-*)
function escapeAttr(str) {
  return escapeHtml(str).replaceAll("`", "&#096;");
}

/* =========================================
   Panel lifecycle & bindings
========================================= */

function afterRender(panelId) {
  if (panelId === "projects") bindProjectView();
  if (panelId === "certificates") initCertificatesInteractions();
  if (panelId === "about") protectAboutImage();
}

function protectAboutImage() {
  const img = overlayBody?.querySelector(".about-image img");
  if (!img) return;

  img.setAttribute("draggable", "false");
  img.addEventListener("dragstart", (e) => e.preventDefault());
  img.addEventListener("contextmenu", (e) => e.preventDefault());
}

function bindProjectView() {
  const viewer = overlayBody.querySelector(".project-viewer");
  const frame = viewer?.querySelector(".project-frame");
  const backBtn = viewer?.querySelector(".project-viewer-back");
  const openBtn = viewer?.querySelector(".project-open-btn");
  const grid = overlayBody.querySelector(".project-grid");

  if (!viewer || !frame || !grid) return;

  overlayBody.querySelectorAll(".project-view-btn").forEach((btn) => {
    btn.addEventListener("click", () => {
      frame.src = btn.dataset.url;
      viewer.hidden = false;
      grid.style.display = "none";
    });
  });

  backBtn?.addEventListener("click", () => {
    frame.src = "";
    viewer.hidden = true;
    grid.style.display = "";
  });

  openBtn?.addEventListener("click", () => {
    const url = frame?.src;
    if (url) window.open(url, "_blank", "noopener");
  });
}

/* ===== Certificates interactions (no leaks) ===== */

let certKeydownHandler = null;
let certClickHandler = null;

function initCertificatesInteractions() {
  const t = tPanel("certificates");
  const earned = t.items.earned || [];
  let currentIndex = -1;

  const viewer = overlayBody.querySelector("#certViewer");
  const list = overlayBody.querySelector("#certList");

  const media = overlayBody.querySelector("#certViewerMedia");
  const img = overlayBody.querySelector("#certViewerImg");
  const title = overlayBody.querySelector("#certViewerTitle");
  const sub = overlayBody.querySelector("#certViewerSub");
  const verify = overlayBody.querySelector("#certViewerVerify");
  const backButton = overlayBody.querySelector('[data-action="cert-back"]');

  const lightbox = overlayBody.querySelector("#certLightbox");
  const lightboxImg = overlayBody.querySelector("#certLightboxImg");
  let listScrollPosition = 0;
  let lastTrigger = null;

  if (!viewer || !list || !media || !img || !title || !sub || !verify || !lightbox || !lightboxImg) return;

  // Remove previous handlers (important for refresh/re-render)
  if (certClickHandler) overlayBody.removeEventListener("click", certClickHandler);
  if (certKeydownHandler) document.removeEventListener("keydown", certKeydownHandler);

  function setLoading(isLoading) {
    media.classList.toggle("is-loading", isLoading);
  }

  function showViewerAt(index) {
    if (index < 0 || index >= earned.length) return;
    currentIndex = index;

    const cert = earned[currentIndex];

    setLoading(true);

    title.textContent = cert.title;
    sub.textContent = `${cert.issuer} • ${cert.year}`;

    verify.href = cert.verifyUrl || "#";
    verify.style.display = cert.verifyUrl && cert.verifyUrl !== "#" ? "inline-flex" : "none";

    img.onload = () => setLoading(false);
    img.onerror = () => setLoading(false);
    img.src = cert.img;
    img.alt = cert.title;

    viewer.hidden = false;
    viewer.classList.add("open");
    viewer.setAttribute("aria-hidden", "false");
    list.hidden = true;
    overlayBody.scrollTo({ top: 0, behavior: "auto" });
    requestAnimationFrame(() => backButton?.focus());
  }

  function openViewerById(certId) {
    const idx = earned.findIndex((c) => c.id === certId);
    if (idx === -1) return;
    listScrollPosition = overlayBody.scrollTop;
    showViewerAt(idx);
  }

  function closeViewer() {
    viewer.classList.remove("open");
    viewer.setAttribute("aria-hidden", "true");
    viewer.hidden = true;
    list.hidden = false;
    currentIndex = -1;
    requestAnimationFrame(() => {
      overlayBody.scrollTo({ top: listScrollPosition, behavior: "auto" });
      lastTrigger?.focus();
    });
  }

  function openLightbox() {
    if (currentIndex < 0) return;
    lightboxImg.src = img.src;
    lightboxImg.alt = img.alt;
    lightbox.hidden = false;
    lightbox.classList.add("open");
    lightbox.setAttribute("aria-hidden", "false");
  }

  function closeLightbox() {
    lightbox.classList.remove("open");
    lightbox.setAttribute("aria-hidden", "true");
    lightbox.hidden = true;
    lightboxImg.src = "";
  }

  function next() {
    if (earned.length === 0) return;
    showViewerAt((currentIndex + 1) % earned.length);
  }

  function prev() {
    if (earned.length === 0) return;
    showViewerAt((currentIndex - 1 + earned.length) % earned.length);
  }

  // prevent drag + context menu (best-effort)
  [img, lightboxImg].forEach((el) => {
    el.setAttribute("draggable", "false");
    el.addEventListener("dragstart", (e) => e.preventDefault());
    el.addEventListener("contextmenu", (e) => e.preventDefault());
  });

  overlayBody.querySelectorAll(".cert-badge img").forEach((thumb) => {
    thumb.setAttribute("draggable", "false");
    thumb.addEventListener("dragstart", (e) => e.preventDefault());
    thumb.addEventListener("contextmenu", (e) => e.preventDefault());
  });

  backButton?.addEventListener("click", (event) => {
    event.preventDefault();
    event.stopPropagation();
    closeViewer();
  });

  certClickHandler = (e) => {
    const actionEl = e.target.closest("[data-action]");
    if (actionEl) {
      const action = actionEl.dataset.action;

      if (action === "cert-view") {
        e.preventDefault();
        lastTrigger = actionEl;
        openViewerById(actionEl.dataset.certId);
        return;
      }
      if (action === "cert-back") {
        e.preventDefault();
        closeViewer();
        return;
      }
      if (action === "cert-next") {
        e.preventDefault();
        next();
        return;
      }
      if (action === "cert-prev") {
        e.preventDefault();
        prev();
        return;
      }
    }

    if (e.target === img) {
      openLightbox();
      return;
    }

    if (e.target.closest("#certLightbox")) {
      closeLightbox();
    }
  };

  certKeydownHandler = (e) => {
    const overlayOpen = overlay?.classList.contains("open");
    if (!overlayOpen) return;

    if (e.key === "Escape") {
      if (lightbox.classList.contains("open")) {
        closeLightbox();
        return;
      }
      if (viewer.classList.contains("open")) {
        closeViewer();
        return;
      }
      return;
    }

    if (viewer.classList.contains("open")) {
      if (e.key === "ArrowRight") next();
      if (e.key === "ArrowLeft") prev();
    }
  };

  overlayBody.addEventListener("click", certClickHandler);
  document.addEventListener("keydown", certKeydownHandler);
}

/* =========================================
   Init & Control functions
========================================= */

function initPanels() {
  overlay = document.getElementById("overlay");
  closeBtn = document.getElementById("overlayClose");
  overlayBody = document.getElementById("overlayBody");

  if (!overlay || !overlayBody) {
    console.warn("initPanels: Overlay-Elemente nicht gefunden");
    return;
  }

  closeBtn?.addEventListener("click", closePanel);

  document.addEventListener("keydown", (e) => {
    if (!overlay.classList.contains("open")) return;

    if (e.key === "Escape") {
      const nestedView = overlayBody.querySelector(".cert-lightbox.open, .cert-viewer.open");
      if (!nestedView) closePanel();
      return;
    }

    if (e.key === "Tab") {
      const focusable = [...overlay.querySelectorAll(
        'button:not([disabled]), a[href], iframe, [tabindex]:not([tabindex="-1"])'
      )].filter((element) => !element.hidden && element.offsetParent !== null);
      if (!focusable.length) return;
      const first = focusable[0];
      const last = focusable[focusable.length - 1];
      if (e.shiftKey && document.activeElement === first) {
        e.preventDefault();
        last.focus();
      } else if (!e.shiftKey && document.activeElement === last) {
        e.preventDefault();
        first.focus();
      }
    }
  });

  overlay.addEventListener("click", (e) => {
    if (e.target === overlay) closePanel();
  });
}

function openPanel(id) {
  if (!overlay || !overlayBody) return;

  const renderer = panelRenderers[id];
  if (!renderer) {
    console.warn(`openPanel: Kein Renderer für Panel "${id}" definiert`);
    return;
  }

  currentPanelId = id;
  previouslyFocused = document.activeElement;
  overlayBody.innerHTML = renderer();
  const heading = overlayBody.querySelector("h2");
  if (heading) heading.id = "panelTitle";
  overlay.classList.add("open");
  overlay.setAttribute("aria-hidden", "false");
  document.body.classList.add("dialog-open");
  afterRender(id);
  requestAnimationFrame(() => closeBtn?.focus());
}

function refreshCurrentPanel() {
  if (!overlay || !overlayBody || !currentPanelId) return;

  const renderer = panelRenderers[currentPanelId];
  if (!renderer) return;

  overlayBody.innerHTML = renderer();
  const heading = overlayBody.querySelector("h2");
  if (heading) heading.id = "panelTitle";
  afterRender(currentPanelId);
}

function closePanel() {
  if (!overlay || !overlayBody) return;

  overlay.classList.remove("open");
  overlay.setAttribute("aria-hidden", "true");
  document.body.classList.remove("dialog-open");
  const frame = overlayBody.querySelector("iframe");
  if (frame) frame.src = "about:blank";
  overlayBody.innerHTML = "";
  currentPanelId = null;
  if (previouslyFocused?.isConnected) previouslyFocused.focus();
  previouslyFocused = null;
}

Object.assign(exports, { initPanels, openPanel, refreshCurrentPanel, closePanel });

},
"js/main.js": (require, exports) => {
const { initBoostControls } = require("js/input/controls.js");
const { initOrbit, updateOrbit, refreshOrbitLabels } = require("js/orbit/orbit-ui.js");
const { initStars, drawStars } = require("js/background/stars.js");
const { initNebula, resizeNebula, drawNebula } = require("js/background/nebula.js");
const { initMeteors, drawMeteors } = require("js/background/meteors.js");
const { initBoostCore, drawBoostCore } = require("js/boost/boost-core.js");
const { initPanels, openPanel, refreshCurrentPanel } = require("js/ui/panels.js");
const { setLang } = require("js/i18n/bilingual.js");
const { tUI } = require("js/i18n/bilingual.js");
/**
 * PANELS
 */
initPanels();   // Overlay & Panel-System initialising

const overlay = document.getElementById("overlay");
const overlayInner = document.querySelector(".overlay-inner");

function isPanelMotionAllowed() {
  return false;
}

if (overlay && overlayInner) {
  overlay.addEventListener("mousemove", (e) => {
    if (!overlay.classList.contains("open")) return;
    if (!isPanelMotionAllowed()) return;

    const rect = overlayInner.getBoundingClientRect();
    const x = (e.clientX - rect.left) / rect.width;
    const y = (e.clientY - rect.top) / rect.height;

    const rotateY = (x - 0.5) * 10;
    const rotateX = (0.5 - y) * 10;

    overlayInner.style.setProperty("--tiltX", `${rotateX}deg`);
    overlayInner.style.setProperty("--tiltY", `${rotateY}deg`);
  });

  overlay.addEventListener("mouseleave", () => {
    overlayInner.style.setProperty("--tiltX", `0deg`);
    overlayInner.style.setProperty("--tiltY", `0deg`);
  });
}


/**
 * Language Switching
 */
function initLanguageSwitch() {
  const buttons = document.querySelectorAll(".lang-switch button");
  if (!buttons.length) return;

  buttons.forEach((btn) => {
    btn.addEventListener("click", () => {
      const lang = btn.dataset.lang;
      setLang(lang);

      // Button-UI update
      buttons.forEach((b) => {
        b.classList.toggle("active", b === btn);
        b.setAttribute("aria-pressed", String(b === btn));
      });

      // Panels new render
      refreshCurrentPanel();

      updateBoostHint();

      // set Orbit-Labels new
      refreshOrbitLabels();
      document.documentElement.lang = lang;
    });
  });
}


// Language Switch activate NOW
initLanguageSwitch();

/* ORBIT */
const orbit = document.getElementById("orbit");
initOrbit(orbit, (pageId) => {
  openPanel(pageId);
});

/* CANVAS SETUP */
const canvas = document.getElementById("stars");
const ctx = canvas.getContext("2d");

let w, h;
function resize() {
  const DPR = Math.min(window.devicePixelRatio || 1, 1.5);
  w = window.innerWidth;
  h = window.innerHeight;
  canvas.width = w * DPR;
  canvas.height = h * DPR;
  ctx.setTransform(DPR, 0, 0, DPR, 0, 0);
  resizeNebula(w, h);
}

window.addEventListener("resize", resize);
resize();

/* GLOBAL SPEED CONTROL */
let speedFactor = 1;
let targetSpeed = 1;

/* BACKGROUND MODULES INITIALISING */
initStars(w, h);
initNebula(w, h);
initMeteors(() => ({ width: w, height: h }));

/* BOOST CORE */
const boostCanvas = document.getElementById("boostCore");
initBoostCore(boostCanvas);

/* CONTROLS */
const starBtn = document.getElementById("starBtn");
const boostHint = document.getElementById("boostHint");

function updateBoostHint() {
  if (boostHint) {
    boostHint.textContent = tUI("boostHint");
  }
  starBtn?.setAttribute("aria-label", tUI("boostLabel"));
}

updateBoostHint();


function setTargetSpeedFn(v) {
  targetSpeed = v;
}

initBoostControls(starBtn, boostHint, setTargetSpeedFn);

/* MAIN LOOP */
const reducedMotion = window.matchMedia("(prefers-reduced-motion: reduce)");
let animationFrame = 0;

function animate() {
  ctx.clearRect(0, 0, w, h);

  const bgGrad = ctx.createLinearGradient(0, 0, 0, h);
  bgGrad.addColorStop(0, "rgb(2, 8, 23)");
  bgGrad.addColorStop(1, "rgb(3, 15, 35)");
  ctx.fillStyle = bgGrad;
  ctx.fillRect(0, 0, w, h);

  speedFactor += (targetSpeed - speedFactor) * 0.05;

  drawNebula(ctx);
  drawStars(ctx, speedFactor, w);
  drawMeteors(ctx, speedFactor);
  updateOrbit(reducedMotion.matches ? 0 : speedFactor);
  drawBoostCore(reducedMotion.matches ? 0 : speedFactor);

  animationFrame = requestAnimationFrame(animate);
}

function startAnimation() {
  if (!animationFrame && !document.hidden) animationFrame = requestAnimationFrame(animate);
}

document.addEventListener("visibilitychange", () => {
  if (document.hidden) {
    cancelAnimationFrame(animationFrame);
    animationFrame = 0;
  } else {
    startAnimation();
  }
});

startAnimation();

}};
  const cache = {};
  function require(id) {
    if (cache[id]) return cache[id];
    if (!modules[id]) throw new Error("Module not found: " + id);
    const exports = {};
    cache[id] = exports;
    modules[id](require, exports);
    return exports;
  }
  require("js/main.js");
})();
